{{define "content"}}
<section>
  <h2>Clients</h2>
  <form method="post" action="/clients/check-expired" style="display:flex; gap:1rem; align-items:center">
    <a href="/clients/new">+ Add client</a>
    <button type="submit" class="secondary">Check expired</button>
  </form>
  {{if .Clients}}
  <table>
    <thead><tr><th>Name</th><th>Expires</th><th>Actions</th></tr></thead>
    <tbody>
      {{range .Clients}}
      <tr>
        <td class="mono">{{.Name}}</td>
        <td>{{if .Expires}}{{.Expires}}{{else}}—{{end}}</td>
        <td>
          <a href="/clients/config?name={{.Name}}">Download</a>
          <form method="post" action="/clients/revoke" style="display:inline" onsubmit="return confirm('Revoke {{.Name}}?')">
            <input type="hidden" name="name" value="{{.Name}}" />
            <button type="submit" class="secondary">Revoke</button>
          </form>
        </td>
      </tr>
      {{end}}
    </tbody>
  </table>
  {{else}}
    <p>No clients yet.</p>
  {{end}}
</section>
{{end}}
{{template "layout" .}}
